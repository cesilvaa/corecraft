# Template de variáveis de ambiente para o ambiente development (regtest).
#
# Convenção de nomes:
#   .env.development  → carregado quando RAILS_ENV=development (padrão)
#   .env.sandbox      → carregado quando RAILS_ENV=sandbox
#
# Para usar:
#   cp .env.example .env.development
#   # preencha as credenciais reais
#   docker compose up
#
# Para sandbox:
#   cp .env.sandbox.example .env.sandbox
#   # preencha as credenciais reais
#   RAILS_ENV=sandbox docker compose up

# Bitcoin RPC — development (regtest, node1, porta 18443)
BITCOIN_RPC_HOST=ubuntu
BITCOIN_RPC_PORT=18443
BITCOIN_RPC_USER=corecraft
BITCOIN_RPC_PASSWORD=your_password_here
BITCOIN_RPC_NETWORK=regtest

# Bitcoin ZMQ — development (node1/regtest, porta 28332)
BITCOIN_ZMQ_HOST=ubuntu
BITCOIN_ZMQ_PORT=28332
BITCOIN_ZMQ_ENABLED=true
